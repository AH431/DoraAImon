# Podcast-Transcript-Analyzer-Skill: Large-Scale Document Handling

## 核心規則：高效處理大量知識庫
當處理長篇書籍或超過 20 萬字的 Markdown/PDF 時，務必遵守以下「分治策略」以確保系統穩定性與 API 效率。
---

## 一、API 流量與配額守則
1. **Gemini 1.5 Flash 限制**：
   - 每分鐘請求次數 (RPM)：15 次。
   - 每日請求次數 (RPD)：1,500 次。
   - 若發生 `429: Too Many Requests`，請暫停 30-60 秒後重試。
2. **Context 使用成本**：
   - 一次讀取整本書 (假設 30 萬 Token) 會消耗較高的記憶體與上下文配額。
   - 避免在同一次會話中反覆發送未經壓縮的超長文本。

## 二、處理整本書的最佳實踐 (Chunking Strategy)
不要將整本書一次性作為一個檔案上傳，請執行以下步驟：

1. **章節拆分**：
   - 將書籍依章節切割為多個較小的 `.md` 或 `.txt` 檔案（例如 `Ch01.md`、`Ch02.md`）。
   - 這是最優化的處理方式，能確保 AI 在回答特定章節問題時，不會受到其他章節內容的干擾。

2. **主題化管理**：
   - 利用 `DoraAImon` 的「解析並建立主題」功能，為每個章節建立書籤。
   - 上傳時選擇該書籍的相關章節，避免「資訊過載 (Information Overload)」。

3. **預處理策略**：
   - 若書中有大量圖片，請先檢查 Markdown 中是否有重複的圖片嵌入代碼。
   - 對於不需要 AI 分析的序言、版權頁，請移除以節省 Context Token。

## 三、操作流程檢查表 (Checklist)
- [ ] **檔案大小檢查**：單一檔案若超過 50MB，建議拆分。
- [ ] **主題分類**：是否已按單元進行命名？
- [ ] **上下文選擇**：是否只選取了當下學習所需的章節（減少不必要的 Context 消耗）？

---
*當您要求分析大量資料時，請務必先詢問我該如何優化輸入內容，以避免不必要的 Token 消耗。*
